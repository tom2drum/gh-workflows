name: Checks
on:
    workflow_dispatch:
    pull_request:
        paths-ignore:
            - 'docs/**'
    push:
        branches:
            - main
        paths-ignore:
            - 'docs/**'

jobs:
    hello:
        name: Hello World
        runs-on: ubuntu-latest
        steps:
            - name: Print message
              run: echo "Hello World"
